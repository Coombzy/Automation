# McKing skill catalog

**Role:** coding + GPU + storage  
**Status:** placeholder — agent should write `MANIFEST.json` + optional `published/` full skills on next mutual-improvement export.

```bash
# On McKing host after git pull
python3  # or run fleet-mutual-improvement Phase 1 with catalog export
```
